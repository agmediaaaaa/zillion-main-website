# Redirects file for Cloudflare Pages
# Place this in your "public" directory

# Redirect root domain to www subdomain (if needed)
# https://yourdomain.com/* https://www.yourdomain.com/:splat 301!

# SPA fallback - handle client-side routing
/* /index.html 200
